# Server configuration
server.port=8081

# PostgreSQL database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/cyberguardian
spring.datasource.username=postgres
spring.datasource.password=Amine123##
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.org.springframework=INFO
logging.level.com.cyberguardian=DEBUG

# WebClient timeout settings
spring.webflux.client.timeout=5000

# Application-specific settings
cyberguardian.ml-service.url=http://localhost:8000
cyberguardian.virustotal.enabled=false
